describe('deepObserve', function() {
	var observed;

	beforeEach(function() {
		observed = {};
	});

	it("should be able to watch simple object and it's values same as regular Object.observe", function() {
		var changes = [];
		//pending();
		expect(true).toBe(true);
	});
});